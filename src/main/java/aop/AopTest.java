package aop;

import java.lang.reflect.Proxy;

public class AopTest {
    public static void main(String[] args) {

       // DefaultAop<SimpleVisitor> h=new DefaultAop<>(simpleVisitor);
      //  Visitor v=(Visitor)Proxy.newProxyInstance(simpleVisitor.getClass().getClassLoader(),simpleVisitor.getClass().getInterfaces(),h);

    }
}
